apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kata-monitor
  namespace: openshift-sandboxed-containers-operator
spec:
  selector:
    matchLabels:
      name: kata-monitor
  endpoints:
  - port: metrics
